# Lessons

- Do not document `npm install <package>` or `npx <package>` as the primary path until the package has actually been published to the npm registry. For unpublished npm-compatible repos, document GitHub install commands first and clearly label npm registry commands as post-publish only.
